/**
 * GpsGate API Documentation
 * GpsGate API service
 *
 * The version of the OpenAPI document: v.1
 * Contact: support@gpsgate.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import Location from './Location';

/**
 * The GeocodeResult model module.
 * @module model/GeocodeResult
 * @version v.1
 */
class GeocodeResult {
    /**
     * Constructs a new <code>GeocodeResult</code>.
     * @alias module:model/GeocodeResult
     */
    constructor() { 
        
        GeocodeResult.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>GeocodeResult</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/GeocodeResult} obj Optional instance to populate.
     * @return {module:model/GeocodeResult} The populated <code>GeocodeResult</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new GeocodeResult();

            if (data.hasOwnProperty('geocoderProviderSource')) {
                obj['geocoderProviderSource'] = ApiClient.convertToType(data['geocoderProviderSource'], 'String');
            }
            if (data.hasOwnProperty('locations')) {
                obj['locations'] = ApiClient.convertToType(data['locations'], [Location]);
            }
            if (data.hasOwnProperty('query')) {
                obj['query'] = ApiClient.convertToType(data['query'], 'String');
            }
            if (data.hasOwnProperty('status')) {
                obj['status'] = ApiClient.convertToType(data['status'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>GeocodeResult</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>GeocodeResult</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['geocoderProviderSource'] && !(typeof data['geocoderProviderSource'] === 'string' || data['geocoderProviderSource'] instanceof String)) {
            throw new Error("Expected the field `geocoderProviderSource` to be a primitive type in the JSON string but got " + data['geocoderProviderSource']);
        }
        if (data['locations']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['locations'])) {
                throw new Error("Expected the field `locations` to be an array in the JSON data but got " + data['locations']);
            }
            // validate the optional field `locations` (array)
            for (const item of data['locations']) {
                Location.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['query'] && !(typeof data['query'] === 'string' || data['query'] instanceof String)) {
            throw new Error("Expected the field `query` to be a primitive type in the JSON string but got " + data['query']);
        }
        // ensure the json data is a string
        if (data['status'] && !(typeof data['status'] === 'string' || data['status'] instanceof String)) {
            throw new Error("Expected the field `status` to be a primitive type in the JSON string but got " + data['status']);
        }

        return true;
    }


}



/**
 * @member {String} geocoderProviderSource
 */
GeocodeResult.prototype['geocoderProviderSource'] = undefined;

/**
 * @member {Array.<module:model/Location>} locations
 */
GeocodeResult.prototype['locations'] = undefined;

/**
 * @member {String} query
 */
GeocodeResult.prototype['query'] = undefined;

/**
 * @member {String} status
 */
GeocodeResult.prototype['status'] = undefined;






export default GeocodeResult;


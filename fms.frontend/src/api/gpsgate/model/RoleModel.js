/**
 * GpsGate API Documentation
 * GpsGate API service
 *
 * The version of the OpenAPI document: v.1
 * Contact: support@gpsgate.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The RoleModel model module.
 * @module model/RoleModel
 * @version v.1
 */
class RoleModel {
    /**
     * Constructs a new <code>RoleModel</code>.
     * @alias module:model/RoleModel
     * @param id {Number} 
     */
    constructor(id) { 
        
        RoleModel.initialize(this, id);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, id) { 
        obj['id'] = id;
    }

    /**
     * Constructs a <code>RoleModel</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/RoleModel} obj Optional instance to populate.
     * @return {module:model/RoleModel} The populated <code>RoleModel</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new RoleModel();

            if (data.hasOwnProperty('description')) {
                obj['description'] = ApiClient.convertToType(data['description'], 'String');
            }
            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'Number');
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('usersIds')) {
                obj['usersIds'] = ApiClient.convertToType(data['usersIds'], ['Number']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>RoleModel</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>RoleModel</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of RoleModel.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['description'] && !(typeof data['description'] === 'string' || data['description'] instanceof String)) {
            throw new Error("Expected the field `description` to be a primitive type in the JSON string but got " + data['description']);
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['usersIds'])) {
            throw new Error("Expected the field `usersIds` to be an array in the JSON data but got " + data['usersIds']);
        }

        return true;
    }


}

RoleModel.RequiredProperties = ["id"];

/**
 * @member {String} description
 */
RoleModel.prototype['description'] = undefined;

/**
 * @member {Number} id
 */
RoleModel.prototype['id'] = undefined;

/**
 * @member {String} name
 */
RoleModel.prototype['name'] = undefined;

/**
 * @member {Array.<Number>} usersIds
 */
RoleModel.prototype['usersIds'] = undefined;






export default RoleModel;


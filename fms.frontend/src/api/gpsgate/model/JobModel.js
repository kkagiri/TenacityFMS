/**
 * GpsGate API Documentation
 * GpsGate API service
 *
 * The version of the OpenAPI document: v.1
 * Contact: support@gpsgate.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import CustomFieldModel from './CustomFieldModel';

/**
 * The JobModel model module.
 * @module model/JobModel
 * @version v.1
 */
class JobModel {
    /**
     * Constructs a new <code>JobModel</code>.
     * @alias module:model/JobModel
     * @param lat {Number} 
     * @param lon {Number} 
     * @param poiId {Number} 
     * @param workerId {Number} 
     */
    constructor(lat, lon, poiId, workerId) { 
        
        JobModel.initialize(this, lat, lon, poiId, workerId);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, lat, lon, poiId, workerId) { 
        obj['lat'] = lat;
        obj['lon'] = lon;
        obj['poiId'] = poiId;
        obj['workerId'] = workerId;
    }

    /**
     * Constructs a <code>JobModel</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/JobModel} obj Optional instance to populate.
     * @return {module:model/JobModel} The populated <code>JobModel</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new JobModel();

            if (data.hasOwnProperty('customFields')) {
                obj['customFields'] = ApiClient.convertToType(data['customFields'], [CustomFieldModel]);
            }
            if (data.hasOwnProperty('description')) {
                obj['description'] = ApiClient.convertToType(data['description'], 'String');
            }
            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'String');
            }
            if (data.hasOwnProperty('jobState')) {
                obj['jobState'] = ApiClient.convertToType(data['jobState'], 'String');
            }
            if (data.hasOwnProperty('lat')) {
                obj['lat'] = ApiClient.convertToType(data['lat'], 'Number');
            }
            if (data.hasOwnProperty('lon')) {
                obj['lon'] = ApiClient.convertToType(data['lon'], 'Number');
            }
            if (data.hasOwnProperty('poiId')) {
                obj['poiId'] = ApiClient.convertToType(data['poiId'], 'Number');
            }
            if (data.hasOwnProperty('routeId')) {
                obj['routeId'] = ApiClient.convertToType(data['routeId'], 'String');
            }
            if (data.hasOwnProperty('scheduledTime')) {
                obj['scheduledTime'] = ApiClient.convertToType(data['scheduledTime'], 'String');
            }
            if (data.hasOwnProperty('workerId')) {
                obj['workerId'] = ApiClient.convertToType(data['workerId'], 'Number');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>JobModel</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>JobModel</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of JobModel.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        if (data['customFields']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['customFields'])) {
                throw new Error("Expected the field `customFields` to be an array in the JSON data but got " + data['customFields']);
            }
            // validate the optional field `customFields` (array)
            for (const item of data['customFields']) {
                CustomFieldModel.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['description'] && !(typeof data['description'] === 'string' || data['description'] instanceof String)) {
            throw new Error("Expected the field `description` to be a primitive type in the JSON string but got " + data['description']);
        }
        // ensure the json data is a string
        if (data['id'] && !(typeof data['id'] === 'string' || data['id'] instanceof String)) {
            throw new Error("Expected the field `id` to be a primitive type in the JSON string but got " + data['id']);
        }
        // ensure the json data is a string
        if (data['jobState'] && !(typeof data['jobState'] === 'string' || data['jobState'] instanceof String)) {
            throw new Error("Expected the field `jobState` to be a primitive type in the JSON string but got " + data['jobState']);
        }
        // ensure the json data is a string
        if (data['routeId'] && !(typeof data['routeId'] === 'string' || data['routeId'] instanceof String)) {
            throw new Error("Expected the field `routeId` to be a primitive type in the JSON string but got " + data['routeId']);
        }
        // ensure the json data is a string
        if (data['scheduledTime'] && !(typeof data['scheduledTime'] === 'string' || data['scheduledTime'] instanceof String)) {
            throw new Error("Expected the field `scheduledTime` to be a primitive type in the JSON string but got " + data['scheduledTime']);
        }

        return true;
    }


}

JobModel.RequiredProperties = ["lat", "lon", "poiId", "workerId"];

/**
 * @member {Array.<module:model/CustomFieldModel>} customFields
 */
JobModel.prototype['customFields'] = undefined;

/**
 * @member {String} description
 */
JobModel.prototype['description'] = undefined;

/**
 * @member {String} id
 */
JobModel.prototype['id'] = undefined;

/**
 * @member {String} jobState
 */
JobModel.prototype['jobState'] = undefined;

/**
 * @member {Number} lat
 */
JobModel.prototype['lat'] = undefined;

/**
 * @member {Number} lon
 */
JobModel.prototype['lon'] = undefined;

/**
 * @member {Number} poiId
 */
JobModel.prototype['poiId'] = undefined;

/**
 * @member {String} routeId
 */
JobModel.prototype['routeId'] = undefined;

/**
 * @member {String} scheduledTime
 */
JobModel.prototype['scheduledTime'] = undefined;

/**
 * @member {Number} workerId
 */
JobModel.prototype['workerId'] = undefined;






export default JobModel;


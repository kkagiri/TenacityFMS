/**
 * GpsGate API Documentation
 * GpsGate API service
 *
 * The version of the OpenAPI document: v.1
 * Contact: support@gpsgate.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import BoundingBox from './BoundingBox';
import Position from './Position';

/**
 * The TrackInfoModel model module.
 * @module model/TrackInfoModel
 * @version v.1
 */
class TrackInfoModel {
    /**
     * Constructs a new <code>TrackInfoModel</code>.
     * @alias module:model/TrackInfoModel
     * @param count {Number} 
     * @param id {Number} 
     * @param totalDistance {Number} 
     * @param updated {String} 
     * @param userId {Number} 
     */
    constructor(count, id, totalDistance, updated, userId) { 
        
        TrackInfoModel.initialize(this, count, id, totalDistance, updated, userId);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, count, id, totalDistance, updated, userId) { 
        obj['count'] = count;
        obj['id'] = id;
        obj['totalDistance'] = totalDistance;
        obj['updated'] = updated;
        obj['userId'] = userId;
    }

    /**
     * Constructs a <code>TrackInfoModel</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/TrackInfoModel} obj Optional instance to populate.
     * @return {module:model/TrackInfoModel} The populated <code>TrackInfoModel</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new TrackInfoModel();

            if (data.hasOwnProperty('boundingBox')) {
                obj['boundingBox'] = BoundingBox.constructFromObject(data['boundingBox']);
            }
            if (data.hasOwnProperty('count')) {
                obj['count'] = ApiClient.convertToType(data['count'], 'Number');
            }
            if (data.hasOwnProperty('endPosition')) {
                obj['endPosition'] = Position.constructFromObject(data['endPosition']);
            }
            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'Number');
            }
            if (data.hasOwnProperty('startPosition')) {
                obj['startPosition'] = Position.constructFromObject(data['startPosition']);
            }
            if (data.hasOwnProperty('totalDistance')) {
                obj['totalDistance'] = ApiClient.convertToType(data['totalDistance'], 'Number');
            }
            if (data.hasOwnProperty('updated')) {
                obj['updated'] = ApiClient.convertToType(data['updated'], 'String');
            }
            if (data.hasOwnProperty('userId')) {
                obj['userId'] = ApiClient.convertToType(data['userId'], 'Number');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>TrackInfoModel</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>TrackInfoModel</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of TrackInfoModel.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // validate the optional field `boundingBox`
        if (data['boundingBox']) { // data not null
          BoundingBox.validateJSON(data['boundingBox']);
        }
        // validate the optional field `endPosition`
        if (data['endPosition']) { // data not null
          Position.validateJSON(data['endPosition']);
        }
        // validate the optional field `startPosition`
        if (data['startPosition']) { // data not null
          Position.validateJSON(data['startPosition']);
        }
        // ensure the json data is a string
        if (data['updated'] && !(typeof data['updated'] === 'string' || data['updated'] instanceof String)) {
            throw new Error("Expected the field `updated` to be a primitive type in the JSON string but got " + data['updated']);
        }

        return true;
    }


}

TrackInfoModel.RequiredProperties = ["count", "id", "totalDistance", "updated", "userId"];

/**
 * @member {module:model/BoundingBox} boundingBox
 */
TrackInfoModel.prototype['boundingBox'] = undefined;

/**
 * @member {Number} count
 */
TrackInfoModel.prototype['count'] = undefined;

/**
 * @member {module:model/Position} endPosition
 */
TrackInfoModel.prototype['endPosition'] = undefined;

/**
 * @member {Number} id
 */
TrackInfoModel.prototype['id'] = undefined;

/**
 * @member {module:model/Position} startPosition
 */
TrackInfoModel.prototype['startPosition'] = undefined;

/**
 * @member {Number} totalDistance
 */
TrackInfoModel.prototype['totalDistance'] = undefined;

/**
 * @member {String} updated
 */
TrackInfoModel.prototype['updated'] = undefined;

/**
 * @member {Number} userId
 */
TrackInfoModel.prototype['userId'] = undefined;






export default TrackInfoModel;


/**
 * GpsGate API Documentation
 * GpsGate API service
 *
 * The version of the OpenAPI document: v.1
 * Contact: support@gpsgate.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The UserIconModel model module.
 * @module model/UserIconModel
 * @version v.1
 */
class UserIconModel {
    /**
     * Constructs a new <code>UserIconModel</code>.
     * @alias module:model/UserIconModel
     * @param iconOffsetX {Number} 
     * @param iconOffsetY {Number} 
     * @param rotatable {Boolean} 
     */
    constructor(iconOffsetX, iconOffsetY, rotatable) { 
        
        UserIconModel.initialize(this, iconOffsetX, iconOffsetY, rotatable);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, iconOffsetX, iconOffsetY, rotatable) { 
        obj['iconOffsetX'] = iconOffsetX;
        obj['iconOffsetY'] = iconOffsetY;
        obj['rotatable'] = rotatable;
    }

    /**
     * Constructs a <code>UserIconModel</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/UserIconModel} obj Optional instance to populate.
     * @return {module:model/UserIconModel} The populated <code>UserIconModel</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new UserIconModel();

            if (data.hasOwnProperty('iconGUID')) {
                obj['iconGUID'] = ApiClient.convertToType(data['iconGUID'], 'String');
            }
            if (data.hasOwnProperty('iconOffsetX')) {
                obj['iconOffsetX'] = ApiClient.convertToType(data['iconOffsetX'], 'Number');
            }
            if (data.hasOwnProperty('iconOffsetY')) {
                obj['iconOffsetY'] = ApiClient.convertToType(data['iconOffsetY'], 'Number');
            }
            if (data.hasOwnProperty('rotatable')) {
                obj['rotatable'] = ApiClient.convertToType(data['rotatable'], 'Boolean');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>UserIconModel</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>UserIconModel</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of UserIconModel.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['iconGUID'] && !(typeof data['iconGUID'] === 'string' || data['iconGUID'] instanceof String)) {
            throw new Error("Expected the field `iconGUID` to be a primitive type in the JSON string but got " + data['iconGUID']);
        }

        return true;
    }


}

UserIconModel.RequiredProperties = ["iconOffsetX", "iconOffsetY", "rotatable"];

/**
 * @member {String} iconGUID
 */
UserIconModel.prototype['iconGUID'] = undefined;

/**
 * @member {Number} iconOffsetX
 */
UserIconModel.prototype['iconOffsetX'] = undefined;

/**
 * @member {Number} iconOffsetY
 */
UserIconModel.prototype['iconOffsetY'] = undefined;

/**
 * @member {Boolean} rotatable
 */
UserIconModel.prototype['rotatable'] = undefined;






export default UserIconModel;


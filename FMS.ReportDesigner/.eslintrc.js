module.exports = {
  root: true,
  extends: ['react-app'],
  rules: {
    // You can add custom rules here
  },
  settings: {
    react: {
      version: 'detect' // This will automatically detect the React version
    }
  }
};

# GpsGate API Client Documentation

This document provides an overview of the files in the `fms.frontend/src/api/gpsgate` directory. These files are auto-generated by OpenAPI Generator and are used to interact with the GpsGate API.

## `ApiClient.js`

This file contains the `ApiClient` class, which is responsible for managing low-level client-server communications, parameter marshalling, and other details of interacting with the GpsGate API. An application should not need to use this class directly. Instead, the `*Api` and model classes provide the public API for the service.

### Key Features:

-   **Base URL:** The `ApiClient` is initialized with a base URL for the GpsGate API.
-   **Authentication:** It handles authentication by allowing you to set an API key.
-   **HTTP Headers:** It allows you to set default HTTP headers for all API calls.
-   **Timeout:** It allows you to set a timeout for all API calls.
-   **Caching:** It includes an option to enable or disable caching for GET requests.
-   **Cookies:** It can be configured to save and return cookies in a Node.js environment.
-   **Parameter Marshalling:** It includes methods for converting parameters to strings, building URLs, and normalizing parameters.
-   **MIME Type Handling:** It includes methods for checking for JSON MIME types and choosing a preferred MIME type.
-   **File Handling:** It can detect and handle file-like parameters.
-   **Request Execution:** The `callApi` method is the core of the `ApiClient`. It builds and sends the HTTP request to the GpsGate API and handles the response.
-   **Deserialization:** It includes a `deserialize` method to convert the API response into the appropriate JavaScript objects.
-   **Date Parsing:** It includes a `parseDate` method to parse ISO-8601 date strings.
-   **Type Conversion:** The `convertToType` method is used to convert data to the specified type.

## `index.js`

This file serves as the main entry point for the GpsGate API client library. It imports and exports all of the API and model classes, making them available for use in the application.

### Key Features:

-   **ApiClient:** It exports the `ApiClient` class.
-   **Models:** It exports all of the model classes, which represent the data structures used by the GpsGate API. These models are located in the `model` subdirectory.
-   **APIs:** It exports all of the API classes, which provide the methods for interacting with the GpsGate API. These API classes are located in the `api` subdirectory.

## `api` Directory

This directory contains the API classes that correspond to the various endpoints of the GpsGate REST API. Each file in this directory represents a different API, such as the `UsersApi`, `DevicesApi`, etc. These classes contain methods that allow you to make calls to the GpsGate API and retrieve data.

## `model` Directory

This directory contains the model classes that represent the data structures used by the GpsGate API. Each file in this directory represents a different model, such as the `UserModel`, `DeviceModel`, etc. These classes are used to serialize and deserialize data that is sent to and received from the GpsGate API.

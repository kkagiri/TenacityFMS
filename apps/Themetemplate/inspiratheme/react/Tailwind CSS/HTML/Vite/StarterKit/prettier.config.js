/**
 * @type {import('prettier').Config}
 */

const config = {
    jsxSingleQuote: true,
    semi: false,
    trailingComma: "es5",
    singleQuote: false,
    tabWidth: 4,
    printWidth: 9999,
    proseWrap: "never",
    // plugins: ['prettier-plugin-tailwindcss'],
}

export default config

---
description: 
globs: 
alwaysApply: true
---

# Your rule content


All Response for CRQS and any CRUD and Queries in returning Errors or validation checkes must use [FMSResponse.cs](mdc:FMS.Application/Common/FMSResponse.cs) 
always have validation check 
FMS.Application/                // Core business logic, DTOs, commands, queries, and services for the FMS system
  Services/                     // Business and domain services for application logic.
  Util/                         // Utility classes and middleware helpers.
  PTSServices/                  // Services related to PTS (details not listed).
  ModelsDTOs/                   // Data Transfer Objects (DTOs) for various domain models.
    Vehicle/                    // DTOs specific to vehicles.
    Site/                       // DTOs for site-related data.
    PTS/                        // DTOs for PTS-related data.
    NaftaATG/                   // DTOs for Nafta ATG integration.
    FMS/                        // DTOs for FMS-specific data.
    ExpectedAVG/                // DTOs for expected average calculations.
    Employee/                   // DTOs for employee-related data.
  MappingProfile/               // AutoMapper or mapping configuration profiles.
  Helpers/                      // Helper classes for common operations.
  Handlers/                     // Command and event handlers for processing actions.
    Interface/                  // Handler interfaces.
    Common/                     // Common handler logic.
    PacketHandlers/             // Handlers for processing data packets.
      Tags/                     // Handlers for tag-related packets.
  Communication/                // Communication services and interfaces.
    Tracker/                    // Device tracking and status helpers.
    WebSocket/                  // WebSocket communication logic.
    SignalR/                    // SignalR communication logic.
    Redis/                      // Redis-based communication helpers.
    HttpPolling/                // HTTP polling communication logic.
    Connection/                 // Connection management helpers.
  CommonInterface/              // Shared interfaces for common functionality.
  Common/                       // Common/shared code used across the application.
  Command/                      // Command classes for CQRS and operations.
    DatabaseCommand/            // Commands for database operations.
      TagMonitoringConfigCmd/   // Commands for tag monitoring configuration.
      ...                       // (Other command subfolders)
    PTSCommand/                 // Commands related to PTS.
  Events/                       // Domain and integration events.
    Pump/                       // Events related to pumps.
      Handler/                  // Handlers for pump events.
  Infrastructure/               // Infrastructure and middleware components.
    ErrorHandling/              // Error handling middleware and models.
    DistCacheTracker/           // Distributed cache tracking helpers.
    ErrorCodeHandling/          // Error code handling logic.
    Exceptions/                 // Custom exception classes.
  Queries/                      // Query classes for CQRS and data retrieval.
    Database/                   // Database query logic.
      FMSQuery/                 // FMS-specific queries.
        TagMonitoringConfigQuery/ // Queries for tag monitoring config.
        ...                     // (Other query subfolders)
      ...                       // (Other database query subfolders)
    GPSGATEServer/              // Queries for GPSGATE server integration.
    PTSQueries/                 // Queries related to PTS.
  Validation/                   // Validation logic and validators.
    PTSValidators/              // Validators for PTS-related data.
      Common/                   // Common validation logic.

FMS.webclient/                  // Likely contains the legacy or alternative web client for FMS (files not yet listed)
FMS.frontend/                   // Modern frontend (React/JS), Redux, UI, and client-side logic (files not yet listed)
FMS.PTS.WindowsService/         // Windows service for PTS integration and background processing (files not yet listed)
FMS.Persistence/                // Data access, repositories, and persistence logic (files not yet listed)
FMS.Deployment/                 // Deployment scripts, configs, and automation for FMS (files not yet listed)
FMS.BackgroundServices/         // Background jobs, schedulers, and hosted services (files no
